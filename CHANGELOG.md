# 更新日志 / Changelog

RF Venture 校准工具（FCPM Calibration Tool）版本更新记录。

安装包下载见 [Releases](https://github.com/Zhaixuanze/FCPM/releases)。

---

## V1.0.14 (2026-08-20)

- 频率校准错点防护体系：线性拟合自动剔除频偏超过阈值的错点（此前单个错点会把拟合系数拖偏，全频段读数引入最大约4.7kHz误差并写入设备）；「设置-FCPM-频率采样过滤」新增「频率拟合抛弃阈值」可调（默认1MHz）
- 频率曲线显示拟合直线（剔除错点后的干净系数，本地重算，不受文件旧系数污染）；频偏错点以红色三角/贴边红箭头标注真实错值，不再静默消失或撑爆坐标轴
- 校准结果-数据表：频偏错点红色标注（仅频率校准功率列）；新增「自动修正频率异常值」选项（默认开启）——校准后自动用拟合预测值替换错点，被修正的点蓝色标注，悬停显示原值→修正值
- 修复：纯验证/快速验证任务会把校准结果-数据表重置为单功率布局、清掉原校准数据展示的问题（校准表只在含校准的任务中重建）
- 设置-FCPM 卡片内复选框组间距收紧；校准选项复选框组间距微调

## V1.0.13 (2026-08-14)

- **修复**：校准后更换 FCPM 导致 GPIB 报错的问题（三重修复）——协方差监控与校准互斥（并发双会话会打挂信号源）、校准结束自动发送 IFC 清理总线、测试/手动指令前自动 IFC+SDC 自愈
- GPIB 测试失败时给出针对性处置提示（区分"适配器被 USB 拔插扰动"与"信号源状态机卡死"），GPIB 日志页输出 [diag] 分阶段诊断记录
- **重要**：若更换 FCPM 后 GPIB 仍偶发报错，请将 FCPM 与 82357B USB-GPIB 适配器分接到不同的 USB 接口（同 HUB 时 FCPM 拔插会扰动适配器，约 1~2 分钟自愈）
- FCPM-6000 型号默认校准功率范围从 -20~0 dBm 扩展为 -30~+10 dBm；闸门时间默认值从 1000 ms 调整为 2000 ms（上限同步放宽至 10000 ms）

## V1.0.12 (2026-08-14)

- **修复**：验证结果数据覆盖「校准结果-数据表」的问题——运行验证后校准结果表保留原校准数据，验证数据只在「验证结果」页显示
- 验证功率误差阈值默认值从 1.0 dB 收紧为 0.5 dB（已自定义过的用户设置不受影响）
- FCPM-1000 型号默认校准功率范围从 -20~0 dBm 扩展为 -30~+10 dBm

## V1.0.11 (2026-08-11)

- 新增「协方差」监控页（验证结果与运行日志之间）：实时监控 FCPM 功率与参考功率计读数的归一化协方差（Pearson，右轴 -1~1）
- 协方差页可直接设置信号源频率/功率（默认 1GHz / 0dBm），开始监控后后端持续读参考功率计并控制信号源输出；支持 NRP-Z USB 与 GPIB 两种参考功率计
- FCPM 功率按参考读数窗口取平均、与参考同频刷新；四张数值卡（FCPM功率/参考功率/功率差值/归一化协方差）+ 三曲线叠加（FCPM/参考 左轴 dBm，归一化协方差 右轴）
- Dashboard 频率显示精确到 1Hz（GHz 档 9 位小数，如 1.000000000 GHz）
- 修复「校准+写入+快速验证」验证曲线功率标签错位（0dBm 实测数据显示成功率起点）的显示 bug
- 校准选项「数据」区按钮右对齐；打包版关闭 DevTools 与 Ctrl+R（开发版保留）

## V1.0.10 (2026-08-11)

- Dashboard 新增「功率 vs 时间」实时曲线：滚轮调节时间窗(10µs~10s,1-2-5档位)、Ctrl+滚轮缩放Y轴(每格±5dB)、左键拖动平移(带惯性)、双击复位，实时显示采样率(pt/s)
- 新增功率温度补偿：校准表记录校准时温度与温漂系数，设备按温度实时补偿功率读数；.cal 文件保存真实校准温度；新增「功率温度补偿」设置卡（更新温度偏移并写入设备）
- 界面美化（Apple 风格打磨）：弹窗材质化(毛玻璃+上滑进场)、侧栏激活态(淡蓝底+指示条)、按钮/导航按压反馈、等宽数字排版、适配系统"减少动态"偏好
- 曲线渲染优化：min-max 抽稀消除大窗口抽动、曲线两端贴满绘图区、Y轴固定刻度防止拖动抖动

## V1.0.9 (2026-08-08)

- 修复验证时偶发单点 kHz 级频率偏差：跳过切换频点后首个未收敛闸门周期 + 验证多轮平均（"验证平均次数"可调，校准选项→验证）
- 新增"校准+写入+验证"失败手动续接：写入/回读失败时保留校准数据，弹窗手动重新写入，成功后自动继续验证（应对设备固件回读偶发卡死）
- 新增回读超时自动重试（重发回读命令）+ 停止按钮兜底退出（失败时可正常停止，不再只能 Ctrl+R）
- 新增 GPIB 总线复位（IFC）预防，减少 VI_ERROR_NCIC 报错（信号源偶发夺权时无需手动重启）
- 验证数据表改为横轴频率/纵轴功率并加宽格子、固定首列；验证报告新增"设备温度"
- 修复 USB 卡片设备名偶发乱码、窗口在特定尺寸反复抽动；完成提示按任务类型动态显示

## V1.0.8 (2026-08-06)

- 新增 R&S NRP 系列 USB 功率计支持（NRP-Z11 等），系统选项可切换 GPIB / USB 功率计
- **重要**：使用 R&S NRP USB 探头时，FCPM 与探头尽量不要接同一 USB HUB，否则可能出现 FCPM 数据异常等问题
- 校准 + 写入 + 验证流程稳定性优化

## V1.0.7 (2026-08-04)

- 修复验证时频率误差巨大（几十MHz级别）：利用设备端freUpdateIndex确保频率计数器完成新闸门周期后再采样，解决异步时序导致frequencyHB为旧值的问题
- 修复ADC灵敏度1.9倍偏差：改用DebugTelemetry 20ms定时查询获取adcValueOrigin单点值，替代帧内~4000点平均，灵敏度从~550 counts/dBm降至~290（与C++固件一致）
- 修复后端samples-batch多轮平均时samples重复累积bug：改为增量返回（_last_read_index游标）
- 默认驻留时间从200ms调整为500ms，提高功率采样稳定性
- 断开USB连接时清理DebugTelemetry定时器，防止内存泄漏

## V1.0.6 (2026-08-03)

- 修复闸门时间设为1000ms时频率数据异常：dwell等待时间自动取max(驻留时间, 闸门时间)，确保等精度频率计数器完成完整测量周期
- 新增设置→FCPM页面：频率采样过滤（偏差阈值/平均次数/最大重采样次数）和功率采样过滤（平均次数）
- 新增频率异常值过滤：剔除与设定频率偏差超过阈值的采样点，有效样本不足时自动重采样
- 新增频率/功率校准多轮测量平均，提高数据稳定性
- 新增"校准开始前自动应用到设备"选项（默认开启），避免忘记将闸门时间设置到设备
- 修复info-tip悬停提示在非校准选项页面不显示的问题
- 修复校准进度卡片数值变化导致布局抖动的问题

## V1.0.5 (2026-08-03)

- 新增校准参数型号查找表：设置→校准参数，按FCPM型号管理校准参数
- 新增自动应用校准参数：USB连接设备后根据型号自动填充校准参数，未匹配时使用默认参数
- 预置 FCPM-1000 / FCPM-6000 两种型号参数
- 新增"校准前复位仪器"选项（默认关闭），按需发送 *RST 初始化仪器
- 校准进度改为单进度条+百分比+实时步骤文字（频率/功率）
- GPIB日志颜色区分：信号源橙色、功率计紫色
- 实时数据面板（ADC原始值/实时功率/原始频率/温度）正常工作
- 系统选项卡片统一白色风格
- RF连接示意图文案更新
- 修复校准启动报错 name 'instruments' is not defined
- 修复停止按钮不起作用（取消标志被重置、sleep无法中断、前端状态不同步等）
- 修复ADC原始值始终为0（DebugTelemetry payload长度判断错误、PowerSamples未解析ADC数据）
- 修复功率步进默认值无法从10更新为5（settings.js默认值+localStorage缓存）
- 修复校准进度显示信号源功率而非FCPM端口功率
- 优化USB设备下拉列表：隐藏COM口号，只显示型号
- 修复USB连接偶发"reply was never sent"导致连接无响应的问题
- 修复从设备读取校准数据后，校准范围参数被设备数据覆盖的问题

## V1.0.4 (2026-08-02)

- 新增USB自动断线重连：防止USB线接触不良导致校准中断，校准写入期间自动暂停重连
- 新增写入进度百分比显示：进度条右侧实时显示完成百分比
- 优化写入验证结果展示：取消滚动条，平铺显示所有验证字段
- 优化校准页面排版布局
- 优化校准结果数据表斑马纹显示，防止看串行
- 修复校准结果数据表表头显示原始uint16值（如65516.0 dBm）而非实际dBm的问题
- 修复校准选项卡片标题下方双分割线的问题

## V1.0.3 (2026-08-02)

- 新增写入设备进度弹窗：6步进度指示器实时显示写入过程
- 新增写入后回读校验：自动回读设备数据，逐字段比对验证一致性
- 修复写入设备失败时无反馈的问题（弹窗展示错误详情）
- 写入超时从15秒延长至45秒，适配校验回读时间

## V1.0.2 (2026-07-30)

- 修复功率拟合曲线双重单位转换导致 Y 轴数值爆炸的问题
- 修复频率曲线范围过滤不匹配数据，导致曲线为空的问题
- 修复频率校准范围自适应（支持 0.1-1G、0.8-6G 等多型号）
- 修复校准结果 checkbox 无法取消勾选的问题
- 修复 freDelta 垃圾数据及 null 值处理
- 修复设备日志乱码：过滤非 ASCII 可打印字符
- 修复高 DPI 屏幕下 tooltip 吸合位置偏移的问题
- 修复拟合曲线干扰 tooltip 交互的问题
- 修复从设备读取后左侧导航栏高亮不同步的问题
- 新增曲线悬停高亮：鼠标悬停 checkbox 对应曲线加粗突出
- 新增功率曲线统计信息叠加（曲线数量、斜率均值、电压标准差/中位数）
- 新增 tooltip 同时显示实际值与拟合值及偏差
- 新增"显示拟合曲线"开关移至功率曲线页，实时切换
- 优化曲线坐标轴紧凑显示，减少留白
- 优化 checkbox 等宽对齐排版
- 取消曲线切换时的过渡动画

## V1.0.1 (2026-07-30)

- USB 扫描增强：使用 Windows SetupAPI 读取"总线已报告设备描述"，显示真实产品名
- 修复 CommandRequest 帧格式：payload 前缀 opcode（与 SDK 协议一致）

## V1.0.0 (2026-07-29)

- FCPM 功率计校准工具首版发布
- 支持 USB 设备连接与实时读数（功率/频率/闸门时间/采样率/温度）
- 功率校准：单步/完整/快速三种模式 + 二次拟合曲线
- 校准结果：数据表 + 功率/频率曲线图，支持频率筛选
- 校准数据编辑器：导入/导出/读取/写入/在线编辑
- GPIB 仪器管理：信号源/功率计地址 + 型号指令模板
- 手动 SCPI 指令发送 + GPIB 总线实时监控
- 运行日志 + 调试模式
